{
  "name": "ponder",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "set -a; [ -f .env ] && . ./.env; set +a; ponder dev",
    "start": "set -a; [ -f .env ] && . ./.env; set +a; ponder start",
    "db": "set -a; [ -f .env ] && . ./.env; set +a; ponder db",
    "codegen": "set -a; [ -f .env ] && . ./.env; set +a; ponder codegen",
    "serve": "set -a; [ -f .env ] && . ./.env; set +a; ponder serve",
    "lint": "eslint .",
    "typecheck": "tsc"
  },
  "dependencies": {
    "drizzle-kit": "0.22.8",
    "hono": "^4.5.0",
    "pg": "^8.16.3",
    "ponder": "^0.15.18",
    "viem": "^2.21.3"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/pg": "^8.16.0",
    "eslint": "^8.54.0",
    "eslint-config-ponder": "^0.15.18",
    "typescript": "^5.3.2"
  },
  "engines": {
    "node": ">=18.14"
  }
}
