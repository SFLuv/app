QOL updates
 - polling on wallet page every 2s for transaction history, upon new tx found, balance update should be requested from rpc as well.
 - every tab of every page should be indicated with a query parameter, so that tab setting is persisted across refreshes. data dependent to a tab should be re-fetched every time the tab is selected again.
 -